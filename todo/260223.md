# 📋 오늘의 목표 — 2026/02/23 (월) | 1회차

> 주간 목표 참고: [260223To260301.md](./260223To260301.md)

---

## 오늘 할 일

- [x] ERD 설계 — User / Account / Transaction 관계 확정
  - User 1:N Account
  - Account 1:N Transaction
  - 필드 목록 결정 (id, created_at 등 공통 포함)
- [x] ERD 문서화 — `docs/erd.md` 작성 (Mermaid ERD 다이어그램)
- [x] API 스펙 초안 — `docs/api-spec.md` 작성
  - Account: POST /accounts, GET /accounts/{id}
  - Transaction: POST /transactions, GET /transactions/{id}, GET /accounts/{id}/transactions

---

## 오늘 하지 않을 것 (scope out)

- API 실제 구현 (다음 회차)
- 대시보드 연동 (3회차 이후)

---

## 완료 메모

- 기존 `User`, `Account` 엔티티 확인 → audit 필드 없음, `@ManyToOne` 관계 없음 → 2회차 구현 시 수정 필요
- `Transaction` 엔티티 신규 설계 완료 (type: DEPOSIT/WITHDRAWAL/TRANSFER, status: SUCCESS/FAILED/PENDING)
- `Account.accountId` → `id`로 통일 예정 (2회차)
- `Account.balance` 필드 추가 설계 (Kafka 비동기 업데이트 예정)
- API 응답 공통 래퍼 `{ success, data }` / `{ success, error }` 로 확정
- core-api/src 초기화 (내일 백엔드 환경설정부터 처음부터 시작)
