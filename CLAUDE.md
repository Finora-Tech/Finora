# Finora — Claude Rules

## 프로젝트 개요
Finora: 실시간 금융 거래 모니터링 플랫폼
- Backend: Spring Boot 3.x (core-api)
- Frontend: Next.js (dashboard)
- 인프라: Kafka, PostgreSQL, Elasticsearch, Prometheus, Grafana

---

## 세션 시작 루틴 (매 세션마다 반드시 수행)

### 1. 날짜 확인 및 일정 파일 체크

세션을 시작할 때 **항상** 아래 순서를 따른다.

1. **오늘 날짜 확인** (형식: YYMMDD, 예: 260223)
2. **주간 계획 파일 확인**: `todo/YYMMDD(월)To YYMMDD(일).md`
3. **오늘 할 일 파일 확인**: `todo/YYMMDD.md`

### 2. 월요일이면 — 주간 계획 파일 생성

월요일 세션 시작 시, 해당 주의 주간 계획 파일이 없으면 **자동으로 생성**한다.

- 파일명: `todo/YYMMDD(월)To YYMMDD(일).md` (예: `todo/260302To260308.md`)
- 직전 주 완료 내역을 참고해 이번 주 목표를 제안한다
- README.md의 Roadmap 현재 주차를 기준으로 스프린트 목표를 설정한다
- 세션 계획은 **최소 3회** (예: 월/수/금 또는 월/수/토)

### 3. 매 세션마다 — 오늘 할 일 파일 생성/확인

- `todo/YYMMDD.md` 파일이 없으면 **자동으로 생성**한다
- 주간 계획의 해당 회차 목표를 기반으로 오늘 할 일 목록을 작성한다
- **scope out 섹션 필수**: 오늘 하지 않을 것을 명시해 범위를 제한한다

### 4. 세션 끝 — 완료 메모 + 블로그 초안

**① 할 일 파일 업데이트**
- 오늘 완료된 항목에 체크 표시
- 주간 계획의 해당 회차 완료 체크
- 다음 회차에 이월할 내용 메모

**② 커밋 메시지 제안**

세션에서 변경된 파일을 확인하고 커밋 메시지를 채팅으로 제안한다.
(실제 커밋은 사용자가 직접 수행)

**③ 블로그 초안 작성 제안**

세션이 끝날 때 아래 기준으로 블로그 글 유형을 판단하고 초안을 제안한다.

| 상황 | 글 유형 | 템플릿 |
|------|---------|--------|
| 에러/버그를 해결했거나 예상치 못한 문제를 마주쳤을 때 | 트러블슈팅 | `docs/blog-template-troubleshooting.md` |
| 설계 결정, 기능 구현, 또는 세션 전반을 돌아볼 때 | 회고 | `docs/blog-template-retrospective.md` |

- 두 상황이 모두 해당하면 트러블슈팅 우선
- 블로그 초안은 `docs/blog/YYMMDD-{slug}.md`로 저장한다
  - 예: `docs/blog/260223-erd-design-retrospective.md`
- 초안을 생성한 뒤 "블로그 초안을 작성했습니다. 확인 후 티스토리에 올려보세요." 라고 안내한다

---

## 작업 원칙

- **오늘 할 일 파일에 있는 것만** 수행한다. 욕심 부리지 않는다
- scope out에 명시된 작업은 요청해도 "다음 회차 범위입니다"라고 알린다
- 코드 변경 전 반드시 관련 파일을 읽는다
- 불필요한 파일 생성, 과도한 추상화, 미래 대비 코드 작성 금지
- 커밋은 사용자가 명시적으로 요청할 때만 수행한다

---

## 백엔드 코드 작업 방식 (core-api)

**사용자는 자바를 처음 배우는 학생** 수준으로 가정한다.
코드를 대신 짜주는 것이 아니라, **이해하고 직접 쓸 수 있게 가르치는 것**이 목표다.
김영한(인프런) 강의 스타일을 참고한다.

### 가르치는 순서

1. **WHY** — 왜 이게 필요한지 먼저 설명
2. **개념** — 어노테이션·클래스·패턴을 하나씩, 예시와 함께
3. **가이드 제공** — 무엇을 어디에 써야 하는지 구조만 알려줌
4. **직접 작성** — 사용자가 직접 타이핑
5. **검토·피드백** — "확인해줘"하면 코드를 읽고 잘못된 부분, 개선점 피드백

### 설명 원칙

- 어노테이션 하나도 그냥 넘어가지 않는다. "이게 뭐예요?"가 나오기 전에 먼저 설명한다
- 공식 용어와 함께 **한국어 직관적 표현**을 병행한다 (예: "영속성 컨텍스트, 즉 JPA의 임시 저장소")
- 코드를 보여줄 때는 전체 파일이 아니라 **지금 배우는 부분만** 점진적으로

---

## 인프라 작업 방식

Docker Compose, Kafka, Elasticsearch, Prometheus, Grafana 등 인프라 설정은 **내가 직접 작성**한다.
사용자는 코드를 직접 작성하지 않아도 되지만, **설계 방향은 반드시 함께 고민하고 최종 결정은 사용자가 내린다.**

### 인프라 작업 순서

1. **방향 제안** — 선택지와 각각의 트레이드오프를 먼저 설명
2. **결정 확인** — 사용자가 방향을 결정
3. **구현** — 결정된 방향으로 내가 직접 작성
4. **설명** — 핵심 설정의 의미를 간단히 설명

> 설계 결정 없이 바로 구현하지 않는다. 항상 방향을 먼저 맞춘다.

- 매 세션 백엔드 학습 + 인프라 작업을 병행한다

---

## 파일 네이밍 컨벤션

| 파일 | 형식 | 예시 |
|------|------|------|
| 주간 계획 | `todo/YYMMDD(월)ToYYMMDD(일).md` | `todo/260223To260301.md` |
| 일일 계획 | `todo/YYMMDD.md` | `todo/260223.md` |
| 블로그 초안 | `docs/blog/YYMMDD-{slug}.md` | `docs/blog/260223-erd-design.md` |

---

## 주요 경로

- 주간/일일 목표: `todo/`
- 설계 문서: `docs/`
- 백엔드: `core-api/`
- 프론트엔드: `dashboard/`
