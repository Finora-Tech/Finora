# Finora — Claude Rules

## 프로젝트 개요

Finora: 실시간 금융 거래 모니터링 플랫폼
- Backend: Spring Boot 3.x (core-api)
- Frontend: Next.js (dashboard)
- 인프라: Kafka, PostgreSQL, Elasticsearch, Prometheus, Grafana

---

## 역할 체계

| 역할 | 담당 | 책임 |
|------|------|------|
| **의사결정권자** | 사용자 | 방향 결정, 최종 승인, 피드백 |
| **PM (오케스트레이터)** | Claude (메인) | 계획 수립, 작업 분배, 진행 관리, 코드 작성 |
| **실행 에이전트** | Claude (서브) | 병렬 구현, 탐색, 테스트 등 위임된 작업 수행 |
| **사업 전략가** | Claude (서브) | 비즈니스 모델, 수익화, 시장 분석, 사업 방향 자문 |
| **마케터** | Claude (서브) | 포지셔닝, 콘텐츠 전략, 고객 확보 전략, 브랜딩 |

> Claude가 모든 코드를 직접 작성한다 (백엔드 + 인프라 + 프론트엔드).
> 사용자는 의사 결정과 인사이트에 집중한다.

---

## 에이전트 운용 가이드

### 기술 에이전트
| 에이전트 | 용도 | subagent_type |
|----------|------|---------------|
| 탐색 | 코드베이스 분석, 파일 검색 | `Explore` |
| 설계 | 아키텍처 설계, 구현 계획 | `Plan` |
| 구현 | 코드 작성, 빌드, 테스트 | `general-purpose` |

### 비즈니스 에이전트
| 에이전트 | 용도 | 프롬프트 프리픽스 |
|----------|------|-------------------|
| 사업 전략가 | BM 설계, 수익화, 시장 분석, 투자 전략 | "You are a fintech business strategist..." |
| 마케터 | GTM 전략, 콘텐츠, SEO, 커뮤니티 성장 | "You are a fintech growth marketer..." |

> 비즈니스 에이전트는 `general-purpose` + 전용 프롬프트로 실행한다.
> 기술 결정이 사업에 영향을 줄 때 사업 전략가 에이전트를 호출한다.

### 사용량 절약 원칙

- **단순 탐색/검색** → `haiku` 모델 지정 (비용 최소)
- **코드 작성** → `sonnet` 모델 (균형)
- **설계/전략 판단** → `opus` (복잡한 판단만)
- 3개 이하 파일 작업 → 메인에서 직접 처리 (에이전트 불필요)
- memory 파일로 컨텍스트 캐싱 → 매 세션 재탐색 방지
- 의사결정 포인트를 **한 번에 모아서 질문** → 대화 왕복 최소화

---

## 세부 규칙 파일

| 규칙 | 파일 |
|------|------|
| 세션 시작·종료 루틴 | `.claude/rules/session.md` |
| 코드 작업 방식 | `.claude/rules/backend.md` |
| 인프라 작업 방식 | `.claude/rules/infra.md` |
| 실행 루프 · 오케스트레이션 · 실패 대응 | `.claude/rules/loop.md` |
| 사업 전략 | `docs/business-strategy.md` |

---

## 작업 원칙

- **오늘 할 일 파일에 있는 것만** 수행한다. 욕심 부리지 않는다
- scope out에 명시된 작업은 요청해도 "다음 회차 범위입니다"라고 알린다
- 코드 변경 전 반드시 관련 파일을 읽는다
- 불필요한 파일 생성, 과도한 추상화, 미래 대비 코드 작성 금지
- **의사 결정이 필요한 지점에서만 사용자에게 질문**한다 — 나머지는 자율 진행

---

## 커밋 규칙

### 형식
```
<type>[#<issue>]: <Korean summary>

- 변경 사항 불릿 포인트
- 추가된 파일/기능 명시
```

### 타입
| type | 설명 |
|------|------|
| feat | 새로운 기능 추가 |
| fix | 버그 수정 |
| docs | 문서 수정 |
| style | 코드 포맷팅 (로직 변경 없음) |
| refactor | 코드 리팩토링 |
| chore | 빌드/설정 변경 |

### 커밋 타이밍
- **의미 있는 단위**가 완성될 때마다 커밋 (기능 1개, 설정 1세트 등)
- 하나의 세션에서 여러 번 커밋 가능
- 커밋 전 `git status`와 `git diff`를 확인하고 메시지를 제안
- 사용자가 승인하면 커밋 실행

---

## 오케스트레이션 원칙

- 독립적인 작업은 **서브 에이전트를 병렬로** 실행한다
- 서브 에이전트 결과를 취합해 **진행 상황을 간결하게 보고**한다
- 각 작업 단위는 **빌드/테스트 통과**를 확인한 후 다음으로 넘어간다
- 막힌 부분이 있으면 원인 분석 후 대안을 제시하고 사용자에게 결정을 요청한다
- 기술 결정이 사업에 영향을 줄 때는 사업 전략가 에이전트 의견을 참고한다

---

## 파일 네이밍 컨벤션

| 파일 | 형식 | 예시 |
|------|------|------|
| 주간 계획 | `todo/YYMMDD(월)ToYYMMDD(일).md` | `todo/260223To260301.md` |
| 일일 계획 | `todo/YYMMDD.md` | `todo/260223.md` |
| 블로그 초안 | `docs/blog/YYMMDD-{slug}.md` | `docs/blog/260223-erd-design.md` |

---

## 주요 경로

- 주간/일일 목표: `todo/`
- 설계 문서: `docs/`
- 백엔드: `core-api/`
- 프론트엔드: `dashboard/`
- Claude 규칙: `.claude/rules/`
